{% extends 'base.html.twig' %}

{% block body %}<div class="container">
    <h1>Connexion</h1>
    <form>
        <div class="form-group">
            <label for="exampleInputEmail1">Email address</label>
            <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email">

        </div>
        <div class="form-group">
            <label for="exampleInputPassword1">Password</label>
            <input type="password" class="form-control" id="exampleInputPassword1" placeholder="Password">
        </div>

        {{ form_start(contactForm) }}
        {{ form_widget(contactForm) }}
        <div  class="btn"> <button type="submit" class="btn btn-dark">Se connecter</button>
            <a href={{ path('pay') }} type="submit" class="btn btn-dark">Cotiser</a>
            <a href={{ path('register') }} type="submit" class="btn btn-dark">S'inscrire</a></div>
        {{ form_end(contactForm) }}
        <style>
            .btn {
                margin-left: 50px;
                padding-bottom: 5px;
            }
        </style>
    </form>
   </div>
{% endblock %}

{% block title %}

{% endblock %}
